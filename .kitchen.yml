---
driver:
  name: vagrant

provisioner:
  name: chef_zero
  data_path: test/integration/fixtures

platforms:
  - name: ubuntu-14.04

suites:
  - name: checkout
    run_list:
      - recipe[bazaar_test::checkout]
  - name: update
    run_list:
      - recipe[bazaar_test::update]
